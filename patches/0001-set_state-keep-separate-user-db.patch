diff --git a/src/tau2/environment/environment.py b/src/tau2/environment/environment.py
index 0dce3d2..117c20b 100644
--- a/src/tau2/environment/environment.py
+++ b/src/tau2/environment/environment.py
@@ -354,12 +354,22 @@ class Environment:
                 self.tools.update_db(initialization_data.agent_data)
                 # Sync user_tools.db to point to the same db instance as tools.db
                 # This is necessary because update_db creates a new db instance
-                if self.user_tools is not None and self.user_tools.db is not None:
+                # Only when both sides share one DB type (airline/retail). Telecom keeps a separate
+                # TelecomUserDB; overwriting it with the agent TelecomDB breaks every user tool.
+                if (
+                    self.user_tools is not None
+                    and self.user_tools.db is not None
+                    and type(self.user_tools.db) is type(self.tools.db)
+                ):
                     self.user_tools.db = self.tools.db
             if initialization_data.user_data is not None:
                 self.user_tools.update_db(initialization_data.user_data)
                 # Sync tools.db to point to the same db instance as user_tools.db
-                if self.tools is not None and self.tools.db is not None:
+                if (
+                    self.tools is not None
+                    and self.tools.db is not None
+                    and type(self.tools.db) is type(self.user_tools.db)
+                ):
                     self.tools.db = self.user_tools.db
 
         if initialization_actions is not None:
